# Sam v2 Migration Tracker

> Source: `sam_v2/docs/FEATURE_INVENTORY.md`
> Truth-reset date: `2026-05-06`
> Status key: `Not Started` | `Migrated But Unverified` | `Real-Tested` | `Blocked` | `Drop Candidate`
> Rule: a feature is only `Real-Tested` when it has been proven on a real execution path for the kind of thing it claims to do. Broad module existence or in-process script coverage alone does not qualify.

| Feature | Old files | Status | Notes |
|---|---|---|---|
| Diagnostics and logging foundation | `main.py`, `daemon/main.py`, `system/session_logger.py`, `agent/error_handler.py` | Real-Tested | Real file-backed logs are currently proven by `python sam_v2/tests_live/test_diagnostics_live.py`; runtime action/summary logs and worker error logs are written and inspected from disk |
| Structured result and error model | broad old runtime/daemon error handling | Real-Tested | Cross-subsystem result behavior is now validated by `python sam_v2/tests_live/test_result_model_live.py` plus supporting runtime/worker paths; `SamResult` and `ErrorType` are proven across real `success`, `failed`, `needs_approval`, `blocked`, and `partial` execution paths with consistent `next_action` behavior |
| Storage/Vault foundation | `vault/schema.py`, `daemon/vault_routes.py`, `goals/tracker.py`, `pipeline/engine.py` | Real-Tested | Real SQLite DB path tested by `python sam_v2/tests_live/test_vault_live.py`; schema, audit row, task row, and failure path are proven |
| Config loading | `config/*`, `memory/config_manager.py`, `.env`, `llm.py` | Real-Tested | Clean `sam_v2` config loading is now validated by `python sam_v2/tests_live/test_config_live.py`; YAML, `.env`, and `os.environ` overrides are merged on a real local path, and the migrated Ollama client plus daemon server are both proven to read the new config layer correctly |
| Sam v2 app entrypoint | startup wiring, launcher surface, daemon boot files | Real-Tested | The package entrypoint is now validated by `python sam_v2/tests_live/test_main_entry_live.py`; `python -m sam_v2` can boot the runtime, scaffold a project, plan it, execute a delegated task, and report who did what through the real package entrypoint using a real data directory |
| Core assistant runtime loop | `main.py`, `conversation_state.py`, `llm.py`, `tts.py`, `ui.py`, `intents/handlers.py` | Real-Tested | Current runtime conversation slice is now validated by `python sam_v2/tests_live/test_conversation_live.py` plus `python sam_v2/tests_live/test_runtime_live.py`; supported prompts cover chat, project listing, goal clarification, ambiguity handling, repo inspection prompting, and approval-sensitive push gating. TTS/UI/watcher behavior is still outside this validation scope |
| Supervisor architecture (coding/testing/project execution) | `agents/orchestrator.py`, `agents/hierarchy.py`, `agents/delegation.py`, `agent/planner.py`, `agent/executor.py` | Real-Tested | Real project execution is now validated by `python sam_v2/tests_live/test_supervisor_project_live.py`; the supervisor can run a real registered project's test command, handle missing build commands truthfully, and request approval for governed execution |
| Retry based on logs | `agent/executor.py`, `agent/monitor.py`, `agents/delegation.py` | Real-Tested | Real retry behavior is now validated by `python sam_v2/tests_live/test_retry_recovery_live.py` plus `python sam_v2/tests_live/test_workflow_bridge_repo_live.py`; a real flaky command produces a real failed worker result, `RecoveryPolicy` chooses `retry`, and the workflow action log records `step_retrying` before the second attempt succeeds |
| Run workflow steps | `pipeline/engine.py`, `workflows/engine.py`, `agent/task_queue.py` | Real-Tested | Real multi-step workflow execution is now validated by `python sam_v2/tests_live/test_workflow_bridge_live.py` plus `python sam_v2/tests_live/test_workflow_bridge_repo_live.py`; successful multi-step runs, retry behavior, failure classification, and approval-gated pauses are proven on real local command paths |
| Pause and resume workflows | `pipeline/engine.py`, `agent/monitor.py`, `goals/tracker.py` | Real-Tested | Real workflow pause/resume is now validated by `python sam_v2/tests_live/test_pause_resume_workflow_live.py`; a workflow pauses at a state-transition step, persists the remaining steps, resumes later from disk, and completes the remaining real command step successfully |
| Supervisor workflow execution bridge | `agent/planner.py`, `agent/executor.py`, `agent/task_queue.py`, `agents/orchestrator.py` | Blocked | Real in-repo retry/failure behavior is validated by `python sam_v2/tests_live/test_workflow_bridge_repo_live.py` and baseline bridge behavior still passes in `python sam_v2/tests_live/test_workflow_bridge_live.py`. External real-project bridge validation against `attendance-app` remains blocked specifically by the worker subprocess environment: `python sam_v2/tests_live/test_external_project_worker_blocker_live.py` shows the worker fails fast on the external repo write probe, while a direct elevated `flutter test test\\attendance_model_test.dart` in `attendance-app` passes. This means the current gap is external worker execution permissions, not the Flutter project itself |
| Daemon API + dashboard backend | `daemon/main.py`, `daemon/api_routes.py`, `daemon/vault_routes.py`, `daemon/missing_routes.py`, `daemon/extra_routes.py` | Real-Tested | Real localhost daemon behavior is now validated by `python sam_v2/tests_live/test_daemon_server_live.py` plus `python sam_v2/tests_live/test_daemon_live.py`; `/health`, `/api/chat`, websocket broadcast, SQLite audit persistence, and invalid request handling are proven on a real server socket path. UI/dashboard rendering is still not part of this validation |
| Native desktop shell (idle scene + orb + task popup) | `launcher.py`, `start_orb.py`, `orb/*` | Migrated But Unverified | A clean `sam_v2` native shell now exists in `sam_v2/native_ui/*` and is the default launch surface for `python -m sam_v2`, with `--cli` kept for the terminal REPL. Offscreen coverage now includes `python sam_v2/tests_live/test_native_ui_smoke.py`, `python sam_v2/tests_live/test_native_ui_command_stream_live.py`, `python sam_v2/tests_live/test_native_ui_project_actions_live.py`, `python sam_v2/tests_live/test_native_ui_projects_tasks_live.py`, `python sam_v2/tests_live/test_native_ui_approvals_logs_live.py`, and `python sam_v2/tests_live/test_native_ui_capabilities_git_live.py`, proving activation, cleaner assistant-style dashboard responses, draggable dashboard/task windows, preserved popup activity history, visible worker command/output streaming, current-project context, first-class Capabilities/Git State/Projects/Tasks/Approvals/Logs cards, and dashboard action controls (`Capabilities`, `Git State`, `Projects`, `Tasks`, `Approvals`, `Logs`, `Open Folder`, `Run Again`, `Show Status`, `Delegation`, `Progress`). A manual visual/interaction pass is still needed before this can be promoted to `Real-Tested` |
| React dashboard shell | `ui/src/App.tsx`, `ui/src/hooks/*`, `ui/src/pages/*`, `ui/src/components/*` | Not Started | Broad surface; many tabs depend on backend parity |
| Voice capture (Web Speech + websocket) | `speech_to_text_websocket.py`, `websocket_server.py`, `speech_client.html` | Not Started | Wake-word + transcript queue pipeline |
| Desktop launcher/orb shell | `launcher.py`, `start_launcher.*`, `orb/*` | Migrated But Unverified | The new `sam_v2` native shell now replaces this directionally with a fullscreen idle scene, floating orb, slim dashboard, and movable task popup. Offscreen smoke coverage exists in `python sam_v2/tests_live/test_native_ui_smoke.py`, but a manual visual pass is still required before this is treated as fully validated |
| Capability awareness + controlled upgrade proposals | `core/prompt.txt`, `memory/project_index.json`, capability-related prompt rules | Real-Tested | Real runtime-path awareness is now validated by `python sam_v2/tests_live/test_awareness_runtime_live.py` plus `python sam_v2/tests_live/test_awareness_live.py`; Sam can truthfully list available capabilities, disclose missing capabilities, and record approval-gated upgrade proposals without pretending support. Conversation coverage still passes in `python sam_v2/tests_live/test_conversation_live.py` |
| Project registry + identification | `memory/project_index.json`, `system/git_intelligence.py`, project-related runtime heuristics | Real-Tested | Real project registration and runtime-path identification are validated by `python sam_v2/tests_live/test_project_registry_runtime_live.py`, `python sam_v2/tests_live/test_multi_project_registry_runtime_live.py`, `python sam_v2/tests_live/test_identify_project_live.py`, and `python sam_v2/tests_live/test_tic_tac_runtime_live.py`; project listing, exact and partial project selection, branch/test-command exposure, multi-project disambiguation, ambiguous-query clarification, remembered last-project context, and truthful missing-project handling are proven |
| Project scaffolding + named worker attribution | `actions/file_ops.py`, `actions/dev_agent.py`, `agents/delegation.py` | Real-Tested | Real modular scaffolding is now validated by `python sam_v2/tests_live/test_project_scaffold_live.py`; Sam creates a permanent project in `sam_v2/workspace/projects/sam_tic_tac_modular`, separates markup/styles/logic into multiple files, registers the project, remembers the location, reruns it later, and returns a delegation trace showing that `Mason` created each scaffold file while `Pilot` handled the later run command |
| Project planning + delegation reporting | `agent/planner.py`, `agents/delegation.py`, `actions/dev_agent.py` | Real-Tested | Real project planning is now validated by `python sam_v2/tests_live/test_project_planning_live.py`; Sam plans the registered `sam_tic_tac_modular` project into separate `IMPLEMENTATION_PLAN.md`, `TESTING_PLAN.md`, and `DELEGATION.md` artifacts, assigns named workers `Mason`, `Beacon`, and `Pilot`, and can answer `show delegation for project ...` by reading the saved delegation report through the runtime path |
| Execute delegated project task | `agent/executor.py`, `agents/delegation.py`, `actions/dev_agent.py` | Real-Tested | Real delegated execution is now validated by `python sam_v2/tests_live/test_project_execution_live.py`; Sam executes `add score tracking` on the scaffolded project through named workers, applies real file edits, runs a real validation command, and updates `DELEGATION.md` so the saved report shows who completed and verified the work |
| Show project progress | `agent/monitor.py`, `agents/delegation.py`, `system/report_writer.py` | Real-Tested | Real progress reporting is now validated by `python sam_v2/tests_live/test_project_progress_live.py`; Sam summarizes completed implementation items, next work, testing work, and named-worker updates from the saved project planning state through a runtime request |
| Show project status | `agent/monitor.py`, `actions/code_helper.py`, `system/git_intelligence.py`, `system/report_writer.py` | Real-Tested | Real unified status reporting is now validated by `python sam_v2/tests_live/test_project_status_live.py`; through the real `python -m sam_v2` path Sam merges repo inspection, branch/clean-dirty context, changed files, completed implementation milestones, next work, and named-worker updates into one project status response |
| Inspect git state | `system/git_intelligence.py`, `actions/workspace.py`, `actions/code_helper.py` | Real-Tested | Real git-state inspection is now validated by `python sam_v2/tests_live/test_git_state_runtime_live.py`; a Sam-facing runtime request on a registered repo returns branch, clean/dirty state, changed files, staged files, unstaged files, and untracked files, with truthful failure handling for missing projects |
| Repo inspection workflow | `actions/code_helper.py`, `system/git_intelligence.py`, `agent/planner.py` | Real-Tested | Real repo inspection is validated by `python sam_v2/tests_live/test_repo_inspection_live.py` plus `python sam_v2/tests_live/test_external_repo_inspection_live.py`; branch state, working-tree cleanliness, changed-file reporting, repo root listing, important file sampling, and truthful missing-project handling are proven on both `Sam-Agent` and the external `focusflow_flutter` project when Git access is available |
| Run tests and builds | `actions/dev_agent.py`, `agent/executor.py`, `agents/orchestrator.py` | Real-Tested | Real supervisor-driven test/build execution is now validated by `python sam_v2/tests_live/test_run_tests_builds_live.py` plus `python sam_v2/tests_live/test_supervisor_project_live.py`; a controlled local git repo with declared project commands proves successful test and build runs, a truthful failing build path, workflow summary logs, and worker audit events |
| Edit code through worker | `actions/code_helper.py`, `actions/dev_agent.py`, `agents/orchestrator.py` | Real-Tested | Real code editing is now validated by `python sam_v2/tests_live/test_worker_edit_live.py`; a controlled local git repo starts with a failing test, the worker edits the code, returns a unified diff, the test reruns successfully, and SQLite audit events record both the edit and the command execution |
| Summarize diff | `actions/code_helper.py`, `system/report_writer.py` | Real-Tested | Real diff summarization is now validated by `python sam_v2/tests_live/test_diff_summary_live.py`; after a worker makes a real code edit in a local git repo, Sam summarizes the real `git diff` with the correct changed file, added/removed line counts, and a truthful failure path for empty diff input |
| Intent system + capability registry | `core/prompt.txt`, `core/capabilities.py`, `intents/*` | Real-Tested | Real Ollama-backed understanding path and deterministic safety rails are now validated by `python sam_v2/tests_live/test_conversation_live.py` and `python sam_v2/tests_live/test_intents_live.py`; currently proven prompts cover chat, project listing, goal-style help requests, ambiguity clarification, repo inspection prompting, workflow/goal commands, and approval-sensitive push gating |
| Ask approval before push | `authority/*`, `system/git_intelligence.py`, `actions/dev_agent.py` | Real-Tested | Real runtime-path push gating is now validated by `python sam_v2/tests_live/test_push_approval_live.py`; a real repo-context request for `Sam, push the changes` creates a persisted pending `git.push` approval request, returns `needs_approval`, and remains unexecuted until approval is granted |
| Memory subsystem | `memory/memory_manager.py`, `memory/temporary_memory.py`, `memory/session_state.py` | Real-Tested | Real JSON and SQLite-backed audit path tested by `python sam_v2/tests_live/test_memory_live.py`, and real runtime-path persistence is now validated by `python sam_v2/tests_live/test_memory_runtime_live.py`; persistent memory, session state, runtime request history, and audit alignment are proven |
| Safe terminal/file tools | `actions/file_ops.py`, `actions/workspace.py`, `system/git_intelligence.py`, `actions/terminal.py` | Real-Tested | `sam_v2/tools/safe_local.py` is now validated by `python sam_v2/tests_live/test_safe_tools_live.py`; real file read, directory listing, restricted safe command execution, real git inspection, unsafe-command blocking, and audit logging are proven |
| Open project folder | `actions/desktop.py`, `actions/workspace.py`, `launcher.py` | Real-Tested | Real desktop-path opening is now validated by `python sam_v2/tests_live/test_open_project_folder_live.py`; a runtime `open folder for project ...` request opens a real registered local folder through the local machine path, returns the real root path, records the runtime audit intent, and handles a missing project truthfully |
| Open local folders | `actions/desktop.py`, `actions/workspace.py`, `launcher.py` | Real-Tested | Real local folder opening is now validated by `python sam_v2/tests_live/test_open_folder_live.py`; runtime requests for `open downloads`, `open documents`, and `open sam-agent` resolve and open the real machine folders, record the runtime audit intent, and handle a missing folder truthfully |
| Open local files | `actions/file_ops.py`, `actions/desktop.py`, `actions/workspace.py` | Real-Tested | Real local file opening is now validated by `python sam_v2/tests_live/test_open_file_live.py`; runtime requests for `open readme.md` and `open file <explicit path>` open real machine files, return the resolved file path, record the runtime audit intent, and handle a missing file truthfully |
| Inspect workspace duplicates | `actions/workspace.py`, `actions/desktop.py`, `system/task_queue.py` | Real-Tested | Controlled-workspace duplicate inspection is now validated by `python sam_v2/tests_live/test_workspace_cleanup_live.py`; runtime requests like `inspect the workspace and organize it` identify duplicate project/runtime groups, return proposed delete paths, and record the runtime audit intent |
| Cleanup workspace duplicates | `actions/workspace.py`, `actions/desktop.py`, `system/task_queue.py` | Real-Tested | Controlled-workspace cleanup is now validated by `python sam_v2/tests_live/test_workspace_cleanup_live.py`; explicit confirmation via `confirm cleanup workspace duplicates` deletes only duplicate project/runtime artifacts inside the workspace, leaves canonical paths intact, and records the cleanup in runtime metadata |
| Vault/SQLite persistence | `vault/schema.py`, `daemon/vault_routes.py` | Real-Tested | Same underlying storage feature as `Storage/Vault foundation`; kept here for migration continuity |
| Create task | `goals/tracker.py`, `daemon/vault_routes.py` | Real-Tested | Real task creation is now validated by `python sam_v2/tests_live/test_create_task_live.py`; a runtime `create task:` request persists a task row to SQLite, the row is fetched back successfully, and the runtime audit event records the `create_task` intent |
| Track task | `goals/tracker.py`, `system/task_queue.py` | Real-Tested | Real task tracking is now validated by `python sam_v2/tests_live/test_track_task_live.py`; multiple runtime `update task` requests persist task status and notes updates to SQLite, reload correctly, and leave a durable runtime audit trail for the tracking steps |
| Create goal | `goals/tracker.py` | Real-Tested | Real goal creation is now validated by `python sam_v2/tests_live/test_create_goal_live.py`; a runtime `create goal:` request persists a goal row to SQLite, the goal is fetched back successfully, follow-up `list goals` returns it, blank-title failure stays truthful, and the runtime audit event records the `create_goal` intent |
| List goals | `goals/tracker.py` | Real-Tested | Real goal listing is now validated by `python sam_v2/tests_live/test_list_goals_live.py`; a runtime `list goals` request returns real seeded goal titles and counts from SQLite, records the `list_goals` intent in the runtime audit trail, and handles an empty goal store truthfully |
| Task/goal/pipeline workflows | `goals/tracker.py`, `pipeline/engine.py`, related API routes | Real-Tested | Real SQLite-backed workflow behavior is now validated by `python sam_v2/tests_live/test_workflows_runtime_live.py` plus `python sam_v2/tests_live/test_workflows_live.py`; runtime create/list flows for goals and drafts, goal score updates, pipeline review/approve/publish transitions, invalid publish protection, and workflow audit events are proven. Pause/resume is still not a migrated capability, so this validation covers the currently implemented workflow slice only |
| System watchers + presence engine | `system/presence_engine.py`, `system/watchers/*`, `system/event_bus.py` | Not Started | Risky concurrency side effects |
| WhatsApp automation suite | `automation/whatsapp_*`, `assistant/message_reader.py` | Not Started | High fragility and external dependency |
| Comms channels (Telegram/Discord) | `comms/manager.py`, `comms/channels/*` | Not Started | Optional integration layer |
| Tooling agents (code/test/dev) | `agents/*`, `actions/dev_agent.py`, `actions/code_helper.py` | Real-Tested | Real repo-root worker behavior is now validated by `python sam_v2/tests_live/test_tooling_repo_live.py`, `python sam_v2/tests_live/test_workers_live.py`, `python sam_v2/tests_live/test_tic_tac_runtime_live.py`, and `python sam_v2/tests_live/test_project_scaffold_live.py`; named workers now report as `Mason` (code), `Beacon` (test), and `Pilot` (dev), with truthful command execution, failure handling, approval gating, monitor state, audit trail, real file creation, remembered-project run support, and explicit delegation traces on local controlled repos. This still does not prove autonomous code-fix loops or external Flutter repo execution |
| Authority/approval governance | `authority/*`, authority daemon routes | Real-Tested | Real SQLite-backed approval lifecycle tested by `python sam_v2/tests_live/test_approvals_live.py`; decisions, pending queue, approve path, and audit trail are proven |
